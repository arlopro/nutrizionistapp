# =========================================================
# Laravel
# =========================================================
*.log
.env
.env.*
!.env.example
/vendor
/storage/*.key
/storage/pail
/public/storage
/public/hot
/auth.json
Homestead.json
Homestead.yaml

# Bootstrap cache (generated by artisan cache commands)
/bootstrap/cache/*.php
!bootstrap/cache/.gitkeep

# SQLite databases (dev only — prod usa MySQL)
*.sqlite
*.sqlite-shm
*.sqlite-wal
database/*.sqlite
database/*.sqlite-shm
database/*.sqlite-wal

# =========================================================
# PHP / Testing
# =========================================================
.phpunit.result.cache
/.phpunit.cache
.phpactor.json
/coverage
/clover.xml

# =========================================================
# Node / Vite / Vue (build avviene sul server)
# =========================================================
/node_modules
/public/build
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
*.local
.npmrc

# =========================================================
# IDE / editor
# =========================================================
/.idea
/.vscode
!/.vscode/extensions.json
/.fleet
/.nova
/.zed
*.swp
*.swo
*~
.editorconfig.local

# =========================================================
# OS
# =========================================================
.DS_Store
.DS_Store?
._*
.Spotlight-V100
.Trashes
Thumbs.db
desktop.ini
ehthumbs.db

# =========================================================
# Deployment / server
# =========================================================
/public/storage
.deploy_key
*.pem
*.key
!storage/app/.gitkeep
!storage/framework/cache/.gitkeep
!storage/framework/sessions/.gitkeep
!storage/framework/views/.gitkeep
!storage/logs/.gitkeep
